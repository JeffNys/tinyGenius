{% extends 'layout.html.twig' %}

{% block title %}retirer une lesson{% endblock %}

{% block content %}
    <h1>retirer une lesson</h1>
    <form method="post">
    <div class="form-group">
        <label for="teacher_id" class="form-label mt-4">Prof :</label>
        <select class="form-select" name="teacher_id">
            <option value="{{ teacher.id }}" >{{ teacher.title }}</option>
        </select>
    </div>
    <div class="form-group">
        <label for="lesson_id" class="form-label mt-4">Cours :</label>
        <select class="form-select" name="offer_id">
            <option value="" >-- Selectionner un cours --</option>
        {% for lesson in lessons %}
            <option value="{{ lesson.offer_id }}">{{ lesson.name }}</option>
        {% endfor %}
        </select>
    </div>
    <input type="submit" class="btn btn-primary mt-3 mb-3" value="retirer le cours">
</form>
<a href="/teacher/list" class="mt-3">retourner à la liste des profs</a>
{% endblock %}
