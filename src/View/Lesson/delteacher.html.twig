{% extends 'layout.html.twig' %}

{% block title %}retirer un prof{% endblock %}

{% block content %}
    <h1>retirer un prof</h1>
    <form method="post">
    <div class="form-group">
        <label for="lesson_id" class="form-label mt-4">cour :</label>
        <select class="form-select" name="lesson_id">
            <option value="{{ lesson.id }}" >{{ lesson.name }}</option>
        </select>
    </div>
    <div class="form-group">
        <label for="teacher_id" class="form-label mt-4">prof :</label>
        <select class="form-select" name="offer_id">
            <option value="" >-- Selectionner un prof --</option>
        {% for teacher in teachers %}
            <option value="{{ teacher.offer_id }}">{{ teacher.firstname }} {{ teacher.lastname }} - {{ teacher.email }}</option>
        {% endfor %}
        </select>
    </div>
    <input type="submit" class="btn btn-primary mt-3 mb-3" value="retirer le prof">
</form>
<a href="/lesson/list" class="mt-3">retourner à la liste des cours</a>
{% endblock %}
